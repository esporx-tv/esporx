define(["ext/prototype", "ext/scriptaculous", "ext/datepicker", "ext/prototype-date-extensions", "ext/effects"], function() {
    "use strict";
    // just a trick to ensure all deps are properly loaded before any script uses it
   return {}
});